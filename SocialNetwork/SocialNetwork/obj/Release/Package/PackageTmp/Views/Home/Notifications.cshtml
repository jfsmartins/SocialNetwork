@model IEnumerable<SocialNetwork.Models.Convidar>

@{
    ViewBag.Title = "Notificações";
}

<h2>Convites para grupos</h2>
<br />
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Grupo.nome)*@
            Grupo
        </th>
        
        <th>
            @*@Html.DisplayNameFor(model => model.idUtilizadorConvida)*@
            Convidado  por
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.data)*@
            Data
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grupo.nome)
        </td>
     
        <td>
            @Html.DisplayFor(modelItem => item.Utilizador1.username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data)
        </td>
     
        <td>
            @Html.ActionLink("Aceitar", "AcceptInvite", new { id=item.idConvite }) |
            @Html.ActionLink("Recusar", "RefuseInvite", new { id=item.idConvite })
            @*@Html.ActionLink("Delete", "Delete", new { id=item.idConvite })*@
        </td>
    </tr>
}

</table>
